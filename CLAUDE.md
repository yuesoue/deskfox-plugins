# 项目规则

## 文件安全规则（最高优先级）

### 删除
- 任何删除必须先列清单，等用户回复"确认"才执行
- 禁止 rm/rm -rf/del /f/Remove-Item -Force 等直接删除
- 必须走回收站或移动到 D:/回收站_备份/

### 文件操作
- 执行前输出操作清单，等用户确认
- 批量 >5 个文件：先做前 3 个，确认后继续，每批报告进度
- 不覆盖原文件，新文件命名：原名_processed_日期.扩展名
- 目标路径有同名文件时询问用户
- 完成后输出变更日志并保存到 操作日志/ 文件夹
- 不确定用途的文件不动，归入"待人工处理"清单

### 禁区
- 禁止写入/移动/删除：C:/Windows/、C:/Program Files*/、AppData/（除非用户指定）、.git/

### 数据安全
- 不显示完整密码/API Key/身份证号等敏感信息
- 遇到 .env/credentials 等敏感文件提醒用户，不读取
- 重要文件批量操作前建议备份（备份到 D:/备份/）

## 语言规则

### 沟通语言
- **所有回复必须使用中文**，除非遇到以下特殊情况：
  - 引用原始代码或文件内容中的英文
  - 专有名词、技术术语（如函数名、类名、API名称）
  - 用户明确要求使用其他语言
  - 必须引用原文以避免歧义的情况
- 代码中的注释可以根据上下文使用中文或英文，但应保持统一
- 文档和说明性文字必须使用中文
